# Production environment - used by `npm run build`
# In production the Electron app loads static files, so /api won't work.
# We point directly at the local Django server on port 8765.
VITE_API_BASE_URL=http://127.0.0.1:8765/api
